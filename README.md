# my-repo

Repository created by GitHub Copilot.

This repository was empty, so I scaffolded a minimal Android app project so it can start building as an APK.

Requirements:
- JDK 17
- Android SDK
- Gradle installed or use wrapper later

Quick build command (on a machine with Android SDK configured):

```bash
./gradlew assembleDebug
```

If Gradle is not installed globally, install it and then run the same command.
